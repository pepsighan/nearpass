{
  "name": "nearpass",
  "version": "0.1.0",
  "license": "GPL-3.0",
  "scripts": {
    "build": "node contract/compile.js",
    "build:debug": "node contract/compile.js --debug",
    "dev:deploy": "yarn build && near dev-deploy",
    "prod:deploy": "yarn build && near deploy",
    "prestart": "yarn build:debug && yarn dev:deploy",
    "test": "yarn build:debug && cd contract && cargo test -- --nocapture && cd .."
  },
  "devDependencies": {
    "near-cli": "~2.1.1",
    "shelljs": "~0.8.4"
  },
  "dependencies": {
    "near-api-js": "~0.43.1",
    "regenerator-runtime": "~0.13.5"
  }
}
